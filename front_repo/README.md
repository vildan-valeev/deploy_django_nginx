# Settings frontend

# Deploy
```sh
$ chmod +x init-ssl.sh
```
```sh
$ ./init-ssl.sh
```
## dev

```sh
$ docker-compose -f docker-compose.dev.yml up -d --build
```

## prod
```sh
$ docker-compose up -d --build
```
